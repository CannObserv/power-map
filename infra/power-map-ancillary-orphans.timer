[Unit]
Description=Daily power-map role / role_assignment ancillary orphan audit (issues #324, #326)

[Timer]
OnCalendar=daily
Persistent=true
# Spread the run off exact midnight to avoid contention with other daily jobs.
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
