[Unit]
Description=Daily power-map outbox + tombstone TTL prune (issue #204)

[Timer]
OnCalendar=daily
Persistent=true
# Spread the run off exact midnight to avoid contention with other daily jobs.
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
