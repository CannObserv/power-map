# Warranted losses — lines this repo's curation runs had to REWRITE, not move.
# Form: PATH :: WARRANT :: CONTENT  (warrants: retarget rename duplicate disproven default)
# One entry per judged line. Read the replacement before adding one.

# --- #444: RUNBOOKS.md split (audits -> docs/AUDITS.md, triage -> docs/RUNBOOK_DB_TRIAGE.md,
#     #442 seed -> docs/archive/RUNBOOKS_COMPLETED.md). The doc's own preamble advertised
#     "backfills, vocabulary migrations, and the recurring audits" — all three of which this
#     same change moved out — so the sentence was rewritten to name the new destinations.
docs/RUNBOOKS.md :: retarget :: One-off and scheduled data operations: the importer, seeds, backfills, vocabulary
docs/RUNBOOKS.md :: retarget :: migrations, and the recurring audits. Every writer here is dry-run by default and
docs/RUNBOOKS.md :: retarget :: needs `--execute`; the resolver and target-echo rules are in `docs/CONVENTIONS.md`.

# --- #444: AGENTS.md — the SocratiCode prefetch query (377 tokens) is printed verbatim at
#     every SessionStart by .claude/hooks/socraticode-reminder.sh, so the copy in the policy
#     file was paid twice per session. Replaced by a pointer naming that hook.
AGENTS.md :: duplicate :: SocratiCode tools are deferred — schemas aren't loaded at session start. Call `ToolSearch` with this prefetch query

# --- #444 CR findings 2 & 3: two prose `§` cross-references that were already wrong on
#     main and rode the move into docs/AUDITS.md. Neither is a markdown link, so
#     links.dead and check-seams are both blind to them. Refuted by command:
#       grep -c 'Org lifespan bounds on assignments' docs/CONVENTIONS.md -> 0
#       grep -c 'Citations — write semantics'        docs/ANCILLARY.md   -> 0
#     Both sections live in docs/OBSERVATIONS.md (lines 10 and 78), which is also where
#     docs/SCHEMA.md and docs/ADMIN_PANELS.md correctly point.
docs/RUNBOOKS.md :: disproven :: `dissolved`/`merged_with` entity events — see `docs/CONVENTIONS.md`
docs/RUNBOOKS.md :: disproven :: §"Citations — write semantics".

# --- #444: two AGENTS.md pointers whose target moved in this same change.
AGENTS.md :: retarget :: full triage in `docs/RUNBOOKS.md` § Database unreachable (#410)
AGENTS.md :: retarget :: one-off and scheduled data operations: importer, seeds, backfills, vocabulary migrations, audits

# --- #456: three AGENTS.md lines whose pointer target moved in this same change.
#     docs/CONVENTIONS.md §"Operational scripts" moved to docs/RUNBOOKS.md, so the
#     policy file's "Full rules →" line and both index blurbs had to be re-aimed.
#     The rules themselves relocated verbatim — see docs/RUNBOOKS.md.
AGENTS.md :: retarget :: Operational scripts are dry run by default (#402/#399)
AGENTS.md :: retarget :: ingestion, operational-script dry-run rules
AGENTS.md :: retarget :: data operations: importer, seeds, role sweep, TTL prune

# --- #456: the assignment-observation heading was renamed by its move.
#     It left docs/OBSERVATIONS.md as a top-level "## Assignment observations —
#     update semantics & provenance (#311)" and arrived in docs/API_ASSIGNMENTS.md
#     under the existing "## Assignments" parent, so it is now "### Write semantics
#     & provenance (#311)". Body text moved verbatim; only the heading changed.
docs/OBSERVATIONS.md :: rename :: Assignment observations — update semantics & provenance

# --- #456: the CONVENTIONS.md glance-index pointer for assignment observations.
#     Its target moved to docs/API_ASSIGNMENTS.md §"Write semantics & provenance"
#     in this same change; the one-line summary above it is unchanged.
docs/CONVENTIONS.md :: retarget :: Full rules → `docs/OBSERVATIONS.md` §"Assignment observations

# --- #456 CR3: six pointers corrected after round 3 found them misdirecting.
#     All six are the same warrant — this change moved what they named, and the
#     round-2 pass repointed only the references that quoted a section TITLE.
#     These quote a FILE, so grep missed them; each is re-aimed at the doc that
#     now holds the content. Nothing was dropped: see docs/API_ASSIGNMENTS.md
#     §"Write semantics & provenance" and docs/RUNBOOKS.md §"Operational scripts".
AGENTS.md :: retarget :: Each is enforced by a sweep test; read `docs/CONVENTIONS.md` before changing one
AGENTS.md :: retarget :: each has exact rules in the `docs/SCHEMA*.md` family and `docs/OBSERVATIONS.md`. Read them
docs/CONVENTIONS.md :: retarget :: Public API request/response contracts, the API request log, ingestion patterns, and
docs/CONVENTIONS.md :: retarget :: the dry-run discipline every operational script follows. Database tables and
docs/CONVENTIONS.md :: retarget :: invariants live in `docs/SCHEMA.md`, write semantics in `docs/OBSERVATIONS.md`,
docs/CONVENTIONS.md :: retarget :: person-name rules in `docs/NAMES.md`, endpoint behaviour in `docs/PUBLIC_API.md`.

# --- #456 CR3 (finding 20): docs/SCHEMA.md pointed at a recipe that exists in no
#     live doc. Refuted by command:
#       grep -c "loop_scope\|db_pool" docs/CONVENTIONS.md docs/TESTING.md  ->  0, 0
#     CONVENTIONS.md never carried it and TESTING.md has no fixture guidance either;
#     the only live copies are in archival docs/plans/. The line now names the
#     working reference it already cited plus tests/conftest.py.
docs/SCHEMA.md :: disproven :: Full recipe + the `loop_scope="session"` gotcha → `docs/CONVENTIONS.md`
# --- #458 CR round 1 (curating-context) ---------------------------------------
# The reconciliation bullet gained the seed-reconciliation clause; the base line
# is present verbatim as its prefix, so nothing was dropped.
AGENTS.md :: duplicate :: Every inline `CHECK`/`FK`/`ON DELETE` change ships an idempotent reconciliation `DO` block
# Both index entries describe a doc whose contents this same change altered:
# SCHEMA_INDEXES.md gained the seed-reconciliation section, TESTING.md gained the
# endpoint-test client block demoted out of SCHEMA.md.
AGENTS.md :: retarget :: routes to [SCHEMA_INDEXES](docs/SCHEMA_INDEXES.md)
AGENTS.md :: retarget :: how to run each test tier, the integration marker
# The glance pointer for the endpoint-test client: this same change moved the
# block it pointed at (SCHEMA.md § Core rules -> TESTING.md § Endpoint-test client).
docs/SCHEMA.md :: retarget :: Endpoint-test client: prefer the lifespan-less rollback client
# docs/TESTING.md's self-description: the endpoint-test client moved INTO this
# doc in this same change, so "Fixture and client recipes live in docs/SCHEMA.md"
# became false about the client half the moment the block landed.
docs/TESTING.md :: retarget :: How to run each tier: the Python suite and its integration marker
docs/TESTING.md :: retarget :: and its conventions, the marker-gated browser a11y sweep, and the bats shell suite. Fixture and client
docs/TESTING.md :: retarget :: recipes live in `docs/SCHEMA.md`; the a11y rules themselves in `docs/ACCESSIBILITY.md`.
# A plain navigational pointer at the doc that keeps the schema-side rules — it
# describes what SCHEMA.md still holds, not what left it.
